# MARKETNEST FOR SHOPKEEPER

## Installation

### Prerequisite

- NodeJS

### Step-by-Step

- Run `npm install` to install neccessary package.
- Run `npm run dev` to start developing.
- Run `npm run build` and `npm run start` to start production.
